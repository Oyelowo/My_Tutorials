type Role = 'ADMIN' | 'USER' | 'NOBODY'

enum Direction {
    UP = "UP",
    RIGHT = "RIGHT",
    BOTTOM = "BOTTOM",
    LEFT = "LEFT"
}

const lowo : Role = 'ADMIN';
const {UP, RIGHT , BOTTOM , LEFT } = Direction
const lowo2 : Direction =  BOTTOM